import 'package:flutter/material.dart'; //do not push

class AppColors {
  static const Color primaryColor =
      Color(0xFF196EEE); // for openning and loading screens
  static const Color secondaryColor =
      Color(0xFFF9F9F9); // for other screens backgrounds
  static const Color buttonColor =
      Color(0xFF1954EE); // for main buttons (ex: next , submit)
  static const Color buttonColor2 = Color(
      0xFFC2D6F6); // for other buttons (ex: signup with google, back to login)
  static const Color titleColor = Color(0xFF196EEE); // for app bar text
  static const Color headingColor =
      Color(0xFF150B3D); // for headings (not for app bar)

  //(-----will update the required colors in the future-----)

  //static const Color subHeadingColor = Color(0xFF196EEE); // for sub headings (
}
